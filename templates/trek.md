# Trek — <program>

**Status:** open · **Started:** <date> · **Sources:** <inputs the cut was made from, as paths>
**Board:** <artifact URL — every republish of `board.html` passes this as `url:`>

## Why this cut

<Why these boundaries and not others, in 2–4 sentences. Name the alternatives that
were considered and what each traded away. This answers, weeks later, "why is
reading separate from deciding?".>

## Cycles

### 1. <name> — `not started`

**Goal:** <what changes in the world once this cycle is done — one sentence>

**Scope:** <what is in>

**Out:** <what is explicitly excluded — the field that stops this cycle from
swallowing the next one halfway through>

**Depends on:** <which cycles, and what specifically is needed from each>

**Governing:** <handles from Rules and Facts below — e.g. R3, R5, F2>

**Unknowns:** <what this cycle's brainstorm must resolve, including anything waiting
on third parties>

**Done when:** <how you demonstrate this cycle is finished>

**Artifacts:** folder `cycles/1-<slug>/` · branch — · MR —

### 2. <name> — `not started`

<Same fields. Later cycles are written with less knowledge and will be coarser — that
is expected; the entry is sharpened when dispatched. But `Out` and `Governing` are
worth writing today: they keep the preceding cycle inside its boundaries.>

## Rules

<What the program has decided and every later cycle must obey. One line each, with
the cycle that decided it. Handles are permanent; a superseded rule keeps its handle
and its body becomes "superseded by Rn on <date>; was: <one clause>; text: <path>".>

- **R1** (cut) — <rule>

## Facts

<What the program has measured about systems it does not own. One line each, dated,
with the cycle that found it. The full account lives in that cycle's findings file.>

- **F1** (<date>, cycle <n>) — <fact>

## Open questions

<What gates the program rather than one cycle. One line each, with the owner who can
answer it. Closing one turns it into a rule or a fact and replaces the line with a
pointer to that handle.>

- **Q1** (<owner>) — <question>
